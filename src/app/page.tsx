import React, { FC, useMemo } from 'react'; 
import { Home } from '../component/home';
const Wallet: FC = () => {
 
    return (
      <div>
        <Home/>  
      </div>
    );
};

export default Wallet;